# Model artifact policy

Trained model weights and large model artifacts are ignored by Git. Do not commit them unless their licence explicitly permits redistribution and the repository documentation records the source, version, licence, and intended educational use.

Never package secrets, private data, or real identity-document examples with a model artifact. Prefer a reproducible download or training instruction when licensing allows it.
